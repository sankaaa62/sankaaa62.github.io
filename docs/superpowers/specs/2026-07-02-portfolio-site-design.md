# Портфолио-сайт Unity-разработчика — дизайн

Дата: 2026-07-02
Статус: утверждён пользователем

## Цель

Сайт-портфолио Александра Кандакова (Senior Unity Developer) для прикрепления
ссылки к резюме и рассылки рекрутерам. Показывает избранные коммерческие проекты
и архив прототипов BlackGames, продаёт как игры, так и инженерный уровень.

## Ключевые решения

| Решение | Выбор |
|---|---|
| Хостинг | GitHub Pages. Разработка в этом репозитории (MyShowcases); перед первым деплоем репозиторий переименовывается в `sankaaa62.github.io`, чтобы сайт открывался по корневому адресу https://sankaaa62.github.io |
| Домен | Пока без домена — `sankaaa62.github.io`; кастомный домен (kandakov.dev свободен) прикручивается позже без изменения сайта |
| Генератор | Astro (статическая сборка), деплой через GitHub Actions |
| Дизайн | Тёмная игровая тема (крупные арты, видео) + инженерные элементы (моноширинные цифры/стек, метрики) |
| Образцы | kaitomajima.webflow.io (структура: демо-рил, карточки проектов с жанрами, страницы проектов) + facepunch.com (полноширинные арты игр, крупные цифры-статистика, живость/игривость) |
| Языки | EN по умолчанию (`/`), RU на `/ru/`, переключатель в шапке |

## Структура сайта

### Главная страница
1. **Хиро** — имя, «Senior Unity Developer», ключевые метрики: 6+ лет, 10M+/500K+ установок, 800K+ LoC, команда 0→8. Кнопки: CV (EN/RU, pdf), LinkedIn, Telegram.
2. **Избранные проекты** (5 карточек с переходом на страницы проектов):
   - Days After — mobile survival RPG, 10M+ установок (Reaction Games / MY.GAMES)
   - Dead Impact — co-op ARPG на ECS/DOTS, 500K+ установок, кодовая база 800K строк
   - Forsaken Kingdom — PC roguelite auto-battler (Steam, в разработке), единственный программист
   - Mafia Stories: Idle Tycoon — mobile idle F2P, лид команды
   - Last Wish — 3D визуальная новелла (Steam, VK Play), техлид, финалист «Start Game»
3. **Архив прототипов** — сетка ~30 игр BlackGames: карточка с иконкой/кадром, коротким webm-клипом (hover-автоплей), названием и жанром. Без отдельных страниц.
4. **Навыки** — теги из CV (Unity, C#, ECS/DOTS, Multiplayer, и т.д.).
5. **Контакты** — email, Telegram, LinkedIn.

### Страница проекта (только для 5 избранных)
Видео (YouTube-встраивание где есть трейлеры), скриншоты, роль, конкретные
достижения (из CV/LinkedIn), стек, ссылки на сторы/Steam.

## Контент и медиа

- Тексты о проектах составляются из CV (`G:\Саша\Kandakov_CV(en|ru).pdf`) и LinkedIn, на двух языках.
- Медиа архивных проектов копируются (только копирование, оригиналы не трогать)
  из `D:\DevArchive\BlackGames\MyProjects(BlackGames)` — иконки, скриншоты, видео.
- Видео сжимаются в webm 2–4 МБ (ffmpeg), чтобы репозиторий остался < 1 ГБ
  и файлы < 100 МБ (лимиты GitHub).
- CV-файлы кладутся в сайт для скачивания.

## Архитектура

- Astro c content collections: каждый проект = markdown-файл с frontmatter
  (название, роль, годы, стек, метрики, ссылки, медиа) в двух локалях.
- Компоненты: Hero, FeaturedProjectCard, PrototypeGrid, SkillTags, Footer,
  LanguageSwitcher.
- Никакого бэкенда, форм и БД. Полностью статично.
- Деплой: GitHub Actions → GitHub Pages при push в main.

## Интерактив

Пользователь хочет живой, интерактивный сайт (в духе Facepunch). В первую версию входит:

- Интерактивный canvas-фон в хиро, реагирующий на движение мыши (частицы/физика).
- Анимированные счётчики метрик при появлении в зоне видимости (0 → 10M+ и т.д.).
- Карточки прототипов: при наведении — автовоспроизведение webm-клипа + tilt-эффект.
- Scroll-reveal анимации секций, плавные переходы.
- Пасхалка: маленькая играбельная JS-мини-игра на сайте (уместно для геймдев-портфолио).

Все эффекты — лёгкий ванильный JS/CSS без тяжёлых библиотек; сайт остаётся быстрым
и работает без JS (эффекты — прогрессивное улучшение).

Отложено на потом (после запуска): встраивание играбельного Unity WebGL-билда
одного из прототипов (требует пересборки старого проекта под WebGL).

## Обработка ошибок / ограничения

- Если для прототипа нет видео — карточка со статичной иконкой/скриншотом.
- Внешние видео (YouTube) — с постер-кадром, чтобы страница работала и без них.
- Сайт должен корректно работать на мобильных (рекрутеры открывают с телефона).

## Тестирование

- Локальный предпросмотр (`astro dev`) и визуальная проверка обеих локалей.
- Проверка сборки в CI перед деплоем.
- Lighthouse-проверка производительности (тяжёлые медиа — главный риск).

## Итерация 2 (фидбек пользователя после запуска, 2026-07-02)

Утверждено пользователем. Гамма: остаёмся в оранжевой, добавляем фиолетовые/тиловые
пятна фона. GitHub в контакты НЕ добавляем.

1. **Живой фон и шапка** — 2–3 больших размытых цветных пятна (оранж + фиолет/тил)
   с медленной пульсацией, едва заметная шум-текстура (SVG feTurbulence, как у
   образца ivan-murzak.com), лёгкое свечение акцентов. Контакты в Hero и Contact —
   иконками (Telegram, Email, LinkedIn; inline SVG) + кнопка CV.
2. **Ссылки на сайты проектов** — «Website»: days-after.com (Days After),
   deadimpact.com (Dead Impact), lastwish.fun (Last Wish).
3. **Галереи на страницах проектов** — скриншоты, скачанные с официальных
   сайтов/сторов проектов (промо-материалы собственных проектов пользователя),
   сжатые в webp; сетка с лайтбоксом.
4. **Тексты** — переписать описания проектов по LinkedIn-экспорту
   (буллеты, детали: «от tech launch до глобального релиза», «ускорил доставку
   фич ~в 2 раза» и т.п.). RU — близко к экспорту, EN — перевод в том же тоне.
5. **Лайтбокс прототипов** — клик по карточке открывает модалку: крупный клип +
   4–6 скриншотов из архива (отдельный проход харвеста), название/жанр/год.
   Закрытие: Esc / крестик / клик по подложке.
6. **Секция «Обо мне»** — фото пользователя + текст из «Общие сведения»
   LinkedIn, EN/RU. (Файл фото ожидается от пользователя.)

## Итерация 3 (фидбек пользователя, 2026-07-02, вечер)

Решения пользователя: метрики — вариант «масштаб»; секция прототипов — без переименования
(уточнить подпись «собраны мной лично»); секция проектов → «Проекты»/"Projects";
логотип шапки → монограмма AK.

1. **Фон** — анимация должна быть ЗАМЕТНОЙ: пятна медленно дрейфуют/дышат
   (движение позиции/масштаба, не только opacity), шум-текстура видимая.
2. **Метрики** — честный набор: «6+ лет в геймдеве», «10M+ установок у крупнейшего
   проекта», «200+ прототипов выпущено отделом», «0→8 вырастил команду».
3. **Прототипы**:
   - клипы длиннее: на карточке ~20 c, в модалке отдельный длинный ролик (до ~60 c);
     производительность не страдает (preload=none, загрузка по требованию);
   - заменить видео у: evoride, factoryuniverse, idalpetsalon, tiedyeworkshop,
     wordtour, agentstretch, octopusadventure, toothpickrun, growit, urbankings;
   - удалить kickmaster полностью (запись + медиа);
   - junkyard-recycling → «Car Junk Resurrection»: это релиз с 1M+ установок
     (Google Play id=com.zakgodfrey.carjunkresurrection, упомянут в CV);
     бейдж «1M+ installs», ссылка на стор в модалке, скриншоты со стора,
     закрепить первым в сетке; схема прототипов расширяется полями
     link (optional), badge {en,ru} (optional), pin (optional).
4. **Карточки проектов на главной** — обложка из промо-скриншотов (shot-1.webp)
   для проектов без youtube; починить обрезание тега жанра; добавить строку
   «компания · роль». Схема проектов получает поле company.
5. **Страницы проектов** — блок компании/позиции; локальные видео-трейлеры
   со Steam для Forsaken Kingdom и Last Wish (скачать, сжать в webm ≤8МБ,
   поле trailer); официальные видео для Dead Impact/Mafia Stories — если найдутся
   верифицируемо официальные; ещё одна итерация текстов с оглядкой на CV.
6. **Обо мне** — переделать вёрстку: имя и тайтл крупно в секции, ровная сетка,
   осмысленная разбивка на 3 абзаца (путь → ключевые проекты → сейчас).
7. **Шапка** — монограмма AK вместо «~/kandakov».

## Итерация 4 (фидбек пользователя 2026-07-06): дизайн-концепция «Engineer's Showreel»

Пользователь дал карт-бланш на вкус и инициативу. Референс: ivan-murzak.com.
Фото предоставлено: SankaaaFace.jpg в корне (перенести в public/media, исходник в staging).

### Концепция

Тёмная «витрина инженера»: контент — герой, движение — постоянный, но сдержанный
аккомпанемент. Современно, без цыганщины.

**Палитра ролей цвета** (главное новое правило):
- Оранж #ff5c1a — бренд и CTA (кнопки, монограмма, полосы заголовков).
- Тил #2dd4bf — интерактив и навигация (активный пункт scroll-spy, hover-подсветки,
  курсор typewriter, акценты «живых» элементов).
- Фиолет #7c5cff — атмосфера (фоновые пятна, теги категорий второго плана).
- Один доминирующий акцент на блок; зелёный #5dcaa5 остаётся только для метрик-фактов.

**Система движения** (единая, prefers-reduced-motion отключает всё):
- Reveal при прокрутке: fade + подъём 24px; в сетках — stagger 60мс; заголовки
  секций — с полосой-подчёркиванием, растущей слева (оранж).
- Hover-lift: карточки scale(1.02) + усиление бордера/свечения — один класс .lift.
- Фон v3: два слоя пятен в противофазе + лёгкий параллакс: курсор ±2%, скролл ±4%.
- Header: прячется при скролле вниз, появляется при скролле вверх; blur-подложка;
  scroll-spy: активный раздел подсвечен тиловой полоской.
- Hero-визитка: портрет с оранжевым кольцом-глоу; имя появляется посимвольно;
  под ним ротация ролей typewriter'ом (Senior Unity Developer → Team Lead →
  ECS/DOTS · Multiplayer → AI-driven development); CTA (CV, контакты-иконки).
- Skills: интерактивное 3D-облако тегов (canvas-сфера, вращение за курсором,
  размер по важности); fallback — статичные теги (no-JS/reduced-motion).

### Изменения структуры
1. Hero → визитка с портретом (имя живёт ТОЛЬКО в визитке и футере).
2. About: без имени и без фото (портрет в hero), только заголовок и 3 абзаца.
3. Contact → секция «Get In Touch»: крупный призыв, кнопка-email, иконки.
4. Футер: имя + копирайт (убрать «built with Astro») + соцссылки; мини-игра
   прячется в пасхалку — неприметная кнопка-джойстик в футере без подписи.
5. Карточки проектов: платформы — бейджи с иконками (PC / Android / iOS,
   важно показать и ПК и мобайл); роль — кратко (без скобочных уточнений);
   зелёные метрики — максимум 2 на карточку, пересмотр: FK → «Инди · соло»,
   Mafia → «Рефакторинг и процессы» / «AI-пайплайн разработки».
6. Прототипы: эксперимент — карусель вместо плитки (ОТДЕЛЬНЫМ коммитом
   для лёгкого отката).
7. Тексты: убрать букву «ё» во всех RU-текстах; редакторский проход по всему.

## Итерация 5 (фидбек 2026-07-06, вечер): анимации для всех + доводка

**Ключевой инсайт**: у пользователя ОС с prefers-reduced-motion: reduce — все
гейтированные анимации у него не работали. Новая политика движения:
- При reduce отключаются ТОЛЬКО вестибулярные триггеры: параллакс на курсор/скролл
  и keyframes-дрейф фоновых пятен.
- Всё остальное работает ВСЕГДА: посимвольное имя, typewriter + мигающий курсор,
  reveal/stagger, полосы заголовков, hover-bump (.lift + tilt), float портрета,
  сфера навыков (автовращение). Это осознанный трейд-офф для промо-сайта.

Остальное:
1. Шум-текстура фона — УБРАТЬ полностью (не понравилась).
2. Шапка: убрать текст «Kandakov» (только монограмма AK, glow при hover);
   активный пункт nav — тиловый ЦВЕТ текста + подчеркивание.
3. Get In Touch — карточкой (панель: surface-фон, рамка, radius 16, padding),
   как в референсе. Тексты профессиональнее: EN "I'm currently open to new
   opportunities — full-time roles or interesting projects, remote friendly.
   Whether you have a question or just want to say hi, my inbox is open." /
   RU в том же деловом тоне.
4. Футер: ТОЛЬКО строка «© 2026 Aleksandr Kandakov. All rights reserved.»
   (mono, muted, по центру). Клик по строке — запуск мини-игры (пасхалка).
   Джойстик-иконку, имя и SocialIcons из футера убрать.
5. Карточки проектов: метрики разделять « · » (сейчас слипаются);
   Forsaken Kingdom метрики → EN ["Sole programmer", "Systems from scratch"] /
   RU [«Единственный программист», «Все системы с нуля»] (факт: парттайм
   в инди-студии, единственный программист, проектирует все системы);
   бейджи PC/Mobile — оранжевые; жанр и «company · role» поменять местами
   по оформлению и расположению (company·role — чип, жанр — mono-строка).
6. Карусель прототипов: автопрокрутка (медленная, пауза при hover/взаимодействии,
   останов при открытой модалке) + навигация внизу: точки по «страницам»
   (3-4 карточки на точку) + стрелки рядом с точками.

## Вне рамок (пока)

- Кастомный домен (добавим позже: покупка kandakov.dev + CNAME).
- Блог, аналитика, формы обратной связи.
