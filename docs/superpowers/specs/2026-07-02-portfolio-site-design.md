# Портфолио-сайт Unity-разработчика — дизайн

Дата: 2026-07-02
Статус: утверждён пользователем

## Цель

Сайт-портфолио Александра Кандакова (Senior Unity Developer) для прикрепления
ссылки к резюме и рассылки рекрутерам. Показывает избранные коммерческие проекты
и архив прототипов BlackGames, продаёт как игры, так и инженерный уровень.

## Ключевые решения

| Решение | Выбор |
|---|---|
| Хостинг | GitHub Pages. Разработка в этом репозитории (MyShowcases); перед первым деплоем репозиторий переименовывается в `sankaaa62.github.io`, чтобы сайт открывался по корневому адресу https://sankaaa62.github.io |
| Домен | Пока без домена — `sankaaa62.github.io`; кастомный домен (kandakov.dev свободен) прикручивается позже без изменения сайта |
| Генератор | Astro (статическая сборка), деплой через GitHub Actions |
| Дизайн | Тёмная игровая тема (крупные арты, видео) + инженерные элементы (моноширинные цифры/стек, метрики) |
| Образцы | kaitomajima.webflow.io (структура: демо-рил, карточки проектов с жанрами, страницы проектов) + facepunch.com (полноширинные арты игр, крупные цифры-статистика, живость/игривость) |
| Языки | EN по умолчанию (`/`), RU на `/ru/`, переключатель в шапке |

## Структура сайта

### Главная страница
1. **Хиро** — имя, «Senior Unity Developer», ключевые метрики: 6+ лет, 10M+/500K+ установок, 800K+ LoC, команда 0→8. Кнопки: CV (EN/RU, pdf), LinkedIn, Telegram.
2. **Избранные проекты** (5 карточек с переходом на страницы проектов):
   - Days After — mobile survival RPG, 10M+ установок (Reaction Games / MY.GAMES)
   - Dead Impact — co-op ARPG на ECS/DOTS, 500K+ установок, кодовая база 800K строк
   - Forsaken Kingdom — PC roguelite auto-battler (Steam, в разработке), единственный программист
   - Mafia Stories: Idle Tycoon — mobile idle F2P, лид команды
   - Last Wish — 3D визуальная новелла (Steam, VK Play), техлид, финалист «Start Game»
3. **Архив прототипов** — сетка ~30 игр BlackGames: карточка с иконкой/кадром, коротким webm-клипом (hover-автоплей), названием и жанром. Без отдельных страниц.
4. **Навыки** — теги из CV (Unity, C#, ECS/DOTS, Multiplayer, и т.д.).
5. **Контакты** — email, Telegram, LinkedIn.

### Страница проекта (только для 5 избранных)
Видео (YouTube-встраивание где есть трейлеры), скриншоты, роль, конкретные
достижения (из CV/LinkedIn), стек, ссылки на сторы/Steam.

## Контент и медиа

- Тексты о проектах составляются из CV (`G:\Саша\Kandakov_CV(en|ru).pdf`) и LinkedIn, на двух языках.
- Медиа архивных проектов копируются (только копирование, оригиналы не трогать)
  из `D:\DevArchive\BlackGames\MyProjects(BlackGames)` — иконки, скриншоты, видео.
- Видео сжимаются в webm 2–4 МБ (ffmpeg), чтобы репозиторий остался < 1 ГБ
  и файлы < 100 МБ (лимиты GitHub).
- CV-файлы кладутся в сайт для скачивания.

## Архитектура

- Astro c content collections: каждый проект = markdown-файл с frontmatter
  (название, роль, годы, стек, метрики, ссылки, медиа) в двух локалях.
- Компоненты: Hero, FeaturedProjectCard, PrototypeGrid, SkillTags, Footer,
  LanguageSwitcher.
- Никакого бэкенда, форм и БД. Полностью статично.
- Деплой: GitHub Actions → GitHub Pages при push в main.

## Интерактив

Пользователь хочет живой, интерактивный сайт (в духе Facepunch). В первую версию входит:

- Интерактивный canvas-фон в хиро, реагирующий на движение мыши (частицы/физика).
- Анимированные счётчики метрик при появлении в зоне видимости (0 → 10M+ и т.д.).
- Карточки прототипов: при наведении — автовоспроизведение webm-клипа + tilt-эффект.
- Scroll-reveal анимации секций, плавные переходы.
- Пасхалка: маленькая играбельная JS-мини-игра на сайте (уместно для геймдев-портфолио).

Все эффекты — лёгкий ванильный JS/CSS без тяжёлых библиотек; сайт остаётся быстрым
и работает без JS (эффекты — прогрессивное улучшение).

Отложено на потом (после запуска): встраивание играбельного Unity WebGL-билда
одного из прототипов (требует пересборки старого проекта под WebGL).

## Обработка ошибок / ограничения

- Если для прототипа нет видео — карточка со статичной иконкой/скриншотом.
- Внешние видео (YouTube) — с постер-кадром, чтобы страница работала и без них.
- Сайт должен корректно работать на мобильных (рекрутеры открывают с телефона).

## Тестирование

- Локальный предпросмотр (`astro dev`) и визуальная проверка обеих локалей.
- Проверка сборки в CI перед деплоем.
- Lighthouse-проверка производительности (тяжёлые медиа — главный риск).

## Итерация 2 (фидбек пользователя после запуска, 2026-07-02)

Утверждено пользователем. Гамма: остаёмся в оранжевой, добавляем фиолетовые/тиловые
пятна фона. GitHub в контакты НЕ добавляем.

1. **Живой фон и шапка** — 2–3 больших размытых цветных пятна (оранж + фиолет/тил)
   с медленной пульсацией, едва заметная шум-текстура (SVG feTurbulence, как у
   образца ivan-murzak.com), лёгкое свечение акцентов. Контакты в Hero и Contact —
   иконками (Telegram, Email, LinkedIn; inline SVG) + кнопка CV.
2. **Ссылки на сайты проектов** — «Website»: days-after.com (Days After),
   deadimpact.com (Dead Impact), lastwish.fun (Last Wish).
3. **Галереи на страницах проектов** — скриншоты, скачанные с официальных
   сайтов/сторов проектов (промо-материалы собственных проектов пользователя),
   сжатые в webp; сетка с лайтбоксом.
4. **Тексты** — переписать описания проектов по LinkedIn-экспорту
   (буллеты, детали: «от tech launch до глобального релиза», «ускорил доставку
   фич ~в 2 раза» и т.п.). RU — близко к экспорту, EN — перевод в том же тоне.
5. **Лайтбокс прототипов** — клик по карточке открывает модалку: крупный клип +
   4–6 скриншотов из архива (отдельный проход харвеста), название/жанр/год.
   Закрытие: Esc / крестик / клик по подложке.
6. **Секция «Обо мне»** — фото пользователя + текст из «Общие сведения»
   LinkedIn, EN/RU. (Файл фото ожидается от пользователя.)

## Итерация 3 (фидбек пользователя, 2026-07-02, вечер)

Решения пользователя: метрики — вариант «масштаб»; секция прототипов — без переименования
(уточнить подпись «собраны мной лично»); секция проектов → «Проекты»/"Projects";
логотип шапки → монограмма AK.

1. **Фон** — анимация должна быть ЗАМЕТНОЙ: пятна медленно дрейфуют/дышат
   (движение позиции/масштаба, не только opacity), шум-текстура видимая.
2. **Метрики** — честный набор: «6+ лет в геймдеве», «10M+ установок у крупнейшего
   проекта», «200+ прототипов выпущено отделом», «0→8 вырастил команду».
3. **Прототипы**:
   - клипы длиннее: на карточке ~20 c, в модалке отдельный длинный ролик (до ~60 c);
     производительность не страдает (preload=none, загрузка по требованию);
   - заменить видео у: evoride, factoryuniverse, idalpetsalon, tiedyeworkshop,
     wordtour, agentstretch, octopusadventure, toothpickrun, growit, urbankings;
   - удалить kickmaster полностью (запись + медиа);
   - junkyard-recycling → «Car Junk Resurrection»: это релиз с 1M+ установок
     (Google Play id=com.zakgodfrey.carjunkresurrection, упомянут в CV);
     бейдж «1M+ installs», ссылка на стор в модалке, скриншоты со стора,
     закрепить первым в сетке; схема прототипов расширяется полями
     link (optional), badge {en,ru} (optional), pin (optional).
4. **Карточки проектов на главной** — обложка из промо-скриншотов (shot-1.webp)
   для проектов без youtube; починить обрезание тега жанра; добавить строку
   «компания · роль». Схема проектов получает поле company.
5. **Страницы проектов** — блок компании/позиции; локальные видео-трейлеры
   со Steam для Forsaken Kingdom и Last Wish (скачать, сжать в webm ≤8МБ,
   поле trailer); официальные видео для Dead Impact/Mafia Stories — если найдутся
   верифицируемо официальные; ещё одна итерация текстов с оглядкой на CV.
6. **Обо мне** — переделать вёрстку: имя и тайтл крупно в секции, ровная сетка,
   осмысленная разбивка на 3 абзаца (путь → ключевые проекты → сейчас).
7. **Шапка** — монограмма AK вместо «~/kandakov».

## Итерация 4 (фидбек пользователя 2026-07-06): дизайн-концепция «Engineer's Showreel»

Пользователь дал карт-бланш на вкус и инициативу. Референс: ivan-murzak.com.
Фото предоставлено: SankaaaFace.jpg в корне (перенести в public/media, исходник в staging).

### Концепция

Тёмная «витрина инженера»: контент — герой, движение — постоянный, но сдержанный
аккомпанемент. Современно, без цыганщины.

**Палитра ролей цвета** (главное новое правило):
- Оранж #ff5c1a — бренд и CTA (кнопки, монограмма, полосы заголовков).
- Тил #2dd4bf — интерактив и навигация (активный пункт scroll-spy, hover-подсветки,
  курсор typewriter, акценты «живых» элементов).
- Фиолет #7c5cff — атмосфера (фоновые пятна, теги категорий второго плана).
- Один доминирующий акцент на блок; зелёный #5dcaa5 остаётся только для метрик-фактов.

**Система движения** (единая, prefers-reduced-motion отключает всё):
- Reveal при прокрутке: fade + подъём 24px; в сетках — stagger 60мс; заголовки
  секций — с полосой-подчёркиванием, растущей слева (оранж).
- Hover-lift: карточки scale(1.02) + усиление бордера/свечения — один класс .lift.
- Фон v3: два слоя пятен в противофазе + лёгкий параллакс: курсор ±2%, скролл ±4%.
- Header: прячется при скролле вниз, появляется при скролле вверх; blur-подложка;
  scroll-spy: активный раздел подсвечен тиловой полоской.
- Hero-визитка: портрет с оранжевым кольцом-глоу; имя появляется посимвольно;
  под ним ротация ролей typewriter'ом (Senior Unity Developer → Team Lead →
  ECS/DOTS · Multiplayer → AI-driven development); CTA (CV, контакты-иконки).
- Skills: интерактивное 3D-облако тегов (canvas-сфера, вращение за курсором,
  размер по важности); fallback — статичные теги (no-JS/reduced-motion).

### Изменения структуры
1. Hero → визитка с портретом (имя живёт ТОЛЬКО в визитке и футере).
2. About: без имени и без фото (портрет в hero), только заголовок и 3 абзаца.
3. Contact → секция «Get In Touch»: крупный призыв, кнопка-email, иконки.
4. Футер: имя + копирайт (убрать «built with Astro») + соцссылки; мини-игра
   прячется в пасхалку — неприметная кнопка-джойстик в футере без подписи.
5. Карточки проектов: платформы — бейджи с иконками (PC / Android / iOS,
   важно показать и ПК и мобайл); роль — кратко (без скобочных уточнений);
   зелёные метрики — максимум 2 на карточку, пересмотр: FK → «Инди · соло»,
   Mafia → «Рефакторинг и процессы» / «AI-пайплайн разработки».
6. Прототипы: эксперимент — карусель вместо плитки (ОТДЕЛЬНЫМ коммитом
   для лёгкого отката).
7. Тексты: убрать букву «ё» во всех RU-текстах; редакторский проход по всему.

## Итерация 5 (фидбек 2026-07-06, вечер): анимации для всех + доводка

**Ключевой инсайт**: у пользователя ОС с prefers-reduced-motion: reduce — все
гейтированные анимации у него не работали. Решение пользователя (2026-07-07):
**prefers-reduced-motion полностью игнорируется** — ни одна анимация не гейтится,
все эффекты (включая параллакс и дрейф фона) работают у всех всегда.

Остальное:
1. Шум-текстура фона — УБРАТЬ полностью (не понравилась).
2. Шапка: убрать текст «Kandakov» (только монограмма AK, glow при hover);
   активный пункт nav — тиловый ЦВЕТ текста + подчеркивание.
3. Get In Touch — карточкой (панель: surface-фон, рамка, radius 16, padding),
   как в референсе. Тексты профессиональнее: EN "I'm currently open to new
   opportunities — full-time roles or interesting projects, remote friendly.
   Whether you have a question or just want to say hi, my inbox is open." /
   RU в том же деловом тоне.
4. Футер: ТОЛЬКО строка «© 2026 Aleksandr Kandakov. All rights reserved.»
   (mono, muted, по центру). Клик по строке — запуск мини-игры (пасхалка).
   Джойстик-иконку, имя и SocialIcons из футера убрать.
5. Карточки проектов: метрики разделять « · » (сейчас слипаются);
   Forsaken Kingdom метрики → EN ["Sole programmer", "Systems from scratch"] /
   RU [«Единственный программист», «Все системы с нуля»] (факт: парттайм
   в инди-студии, единственный программист, проектирует все системы);
   бейджи PC/Mobile — оранжевые; жанр и «company · role» поменять местами
   по оформлению и расположению (company·role — чип, жанр — mono-строка).
6. Карусель прототипов: автопрокрутка (медленная, пауза при hover/взаимодействии,
   останов при открытой модалке) + навигация внизу: точки по «страницам»
   (3-4 карточки на точку) + стрелки рядом с точками.
7. Hero-визитка = полноэкранный «слайд» (как в референсе): min-height
   100vh − высота шапки, контент по вертикальному центру, внизу ненавязчивый
   scroll-индикатор (стрелка/шеврон с мягкой анимацией, клик → скролл к About).
   В левую колонку под typewriter добавить личный абзац (утверждён пользователем):
   RU «Уже больше шести лет с удовольствием делаю игры для разных платформ —
   как линейный разработчик на крупных сложных проектах и как лид на проектах
   поменьше. Выстраиваю процессы разработки, в том числе с использованием AI.
   Слегка педантичен, легко нахожу общий язык с людьми. Вне работы читаю фэнтези,
   слушаю рок и, конечно, играю в игры.»
   EN "For over six years I've been happily making games for different platforms —
   as a developer on large, complex projects and as a lead on smaller ones.
   I build development processes, including AI-assisted ones. A bit of
   a perfectionist, I get along with people easily. Off work I read fantasy,
   listen to rock and, of course, play games."
   (muted, ~60ch, размер .95-1rem — визитка остается воздушной).

## Итерация 6 «Stellar» (фидбек 2026-07-07): сквозная космическая тема

Пользователь утвердил сквозную тематику: звездное небо, созвездия, космос,
туманности, планеты. Карт-бланш на импровизацию по всему проекту,
но не в ущерб контенту. Правило вкуса прежнее: стильно, без цыганщины.

### Баг (приоритет 0)
- Секция «Навыки» у пользователя ПУСТАЯ в реальном браузере: сфера не рисуется,
  статичный список спрятан (.sr-only). Диагностировать в реальных условиях
  (не headless): вероятные причины — canvas размером 0 при init, гонка шрифта,
  ошибка в проде. Починить с защитой: если canvas не отрисовался, статичный
  список ОБЯЗАН остаться видимым.

### Фон-космос
1. Пятна-туманности ярче (алфа ~×1.8) и с более ощутимой анимацией дрейфа.
2. Звезды (частицы hero-canvas) — на всю высоту страницы: canvas становится
   fixed-слоем на весь вьюпорт позади контента, звезды распределены по экрану,
   при прокрутке — легкий параллакс (звезды смещаются медленнее контента).
3. Постоянная легкая собственная динамика звезд (медленный дрейф/мерцание) +
   после ухода курсора звезды плавно возвращаются к «своим» позициям
   (пружина к домашней точке), не скапливаются.
4. «Падающая звезда» (метеор): при смене активного раздела scroll-spy — короткий
   штрих-пролет с случайной стороны верхней части экрана (canvas тот же), редкий
   и ненавязчивый (не чаще раза в несколько секунд).

### Хедер
5. Выделить в верхнем положении: постоянная полупрозрачная подложка чуть темнее
   фона + тонкая нижняя граница уже в состоянии top (не только при скролле).
6. RU-локаль на узких экранах: nav не должен переноситься на две строки —
   ужать gap/шрифт сильнее или сократить подписи (напр. «Обо мне» → «О себе»?
   НЕТ — лучше уменьшить размер/межбуквенный или скрыть подчеркивания;
   решить по месту, проверить 320-420px на /ru/).

### Визитка
7. Имя: постоянное «звездное» микро-мерцание после появления (например,
   letter-spans с медленной случайной осцилляцией яркости/тени — subtle).
8. Компоновка: уменьшить пустоту. Вариант на усмотрение исполнителя:
   портрет над текстом по центру (как визитка-слайд), текст шире; или сетка
   с текстом в 2 колонки. Сохранить полноэкранность слайда.
9. Новый текст hero.about (RU утвержден, EN — перевод в том же тоне):
   абзац 1: «Уже больше шести лет с удовольствием занимаюсь разработкой игр.
   За это время успел поработать над многомиллионными F2P-хитами и ламповыми
   инди, MMO и гиперказуалками, под мобильные платформы и ПК, в больших
   командах и соло — как линейный разработчик и как лид. Собирал команду
   и выстраивал процессы, проводил собеседования и менторил новичков,
   разгребал нейрослоп и налаживал AI-пайплайн, оптимизировал сетевой код
   и строил конвейер прототипов. И еще много всего интересного...»
   абзац 2 (отдельным блоком, мельче/muted): «Оптимист, слегка педантичен,
   легко нахожу общий язык с людьми. В свободное время читаю фэнтези, слушаю
   рок и, конечно, играю в игры.»

### Метрики → «Обо мне»
10. Блок цифр переносится из-под hero в секцию «Обо мне» (под заголовок).
    Подача на усмотрение: колонки/звездная стилизация (например, цифры
    как «звездные величины» с тонкими линиями-созвездиями между блоками).

### Карточки «Проекты»
11. Крупнее: сетка minmax(340-380px), обложки выше.
12. Бейджи PC/Mobile — светлее оранжевый (#ffb01a или под тему).
13. Убрать название компании с карточек; роль — бейджем рядом с платформами.
14. Hover: бамп (уже есть lift — усилить) + «видео»: слайд-шоу скриншотов
    проекта (смена кадров ~1.2с, fade) поверх обложки; у last-wish — реальный
    короткий muted-клип из trailer.webm (сгенерировать hover-clip.webm ~10с);
    плюс микро-анимация бейджей/заголовка при hover (сдвиг/подсветка).
    (Видео остальных проектов добавим, когда пользователь пришлет ролики.)
15. Страница проекта: кнопки-ссылки на площадки — ВЫШЕ длинного текста
    (сразу после шапки/трейлера); стек-теги — цветные (тил текст/рамка
    или радуга по категориям — на вкус, но не серые); общий стиль блоков
    освежить под тему.

### Карусель прототипов
16. Непрерывная плавная зацикленная прокрутка (marquee, ~30-40px/с) вместо
    постраничного автоплея; пауза при hover/взаимодействии/модалке как раньше.
17. Починить свайп на таче (не работает) — вероятно, конфликт с автоплеем/
    snap; при драге автоплей отключается до конца жеста.
18. Точки-навигацию УБРАТЬ (при marquee бессмысленны). Стрелки можно оставить
    (сдвиг на ~3 карточки).
19. Убрать вертикальную прокрутку внутри секции (артефакт scale карточки —
    добавить вертикальный padding контейнеру/overflow-y visible).
20. Консистентное «раздвигание» соседей при hover на ЛЮБОЙ карточке
    (сейчас работает только на крайних) — например, margin-соседям через
    :has() или JS-класс.
21. Инициатива приветствуется: осовременить подачу (например, выделенная
    карточка крупнее по центру). Модалка остается.

### Get In Touch
22. Убрать оранжевую полоску под заголовком внутри карточки (h2 без .reveal-бара
    или отдельный класс). Оформление: тонкая «созвездная» декорация на фоне
    карточки (несколько точек-звезд, соединенных линиями, статично или
    с легким мерцанием — SVG), больше динамики по вкусу.
23. Текст: «Открыт интересным предложениям и новым возможностям. На связи
    любым удобным способом — выбирайте, какой нравится.» / EN: "Open to
    interesting offers and new opportunities. Reach me any way you like —
    pick whichever works for you." (отшлифовать при реализации).

## Итерация 7 (фидбек 2026-07-08): полировка

1. **Карусель прототипов**: drag-скролл мышкой (pointer-based, grab/grabbing курсор,
   инерция не обязательна) + гарантированно рабочий свайп пальцем на реальном
   телефоне (проверить, что pan-x не блокируется; клик по карточке при этом
   не должен срабатывать после драга — порог ~6px).
2. **Звезды у центра экрана бледнее**: per-star множитель альфы по расстоянию
   от центра вьюпорта (в центральной трети — до ~40% яркости, к краям — 100%),
   плавный градиент, чтобы текст читался.
3. **Мобильные отступы**: контент прижат к краям экрана — корневая причина:
   `.section { padding: 72px 0 }` объявлен после `.container` и обнуляет его
   боковые 20px (известный каскадный конфликт, ранее обходился точечно
   в contact-card). Исправить ГЛОБАЛЬНО: `.section` не должен трогать
   горизонтальные паддинги (padding-block вместо шорткода), убрать точечный
   воркараунд contact-card. Проверить все секции на 320-414px.
4. **Текст визитки короче** (утверждено): новый hero.about RU:
   «Уже больше шести лет с удовольствием делаю игры: от многомиллионных
   F2P-хитов до ламповых инди, от MMO до гиперказуалок, под мобильные и ПК,
   в больших командах и соло. Выстраивал процессы и собирал команду, менторил
   новичков, налаживал AI-пайплайн, оптимизировал сетевой код и строил
   конвейер прототипов.» EN — эквивалент. Блок hero.personality УДАЛИТЬ
   (ключ и разметку).
5. **Метрики: редизайн**: убрать сквозную линию-созвездие и угловые звездочки.
   Чистая подача: числа + подписи, тонкие вертикальные разделители между
   колонками (border, не на мобильной сетке 2x2 — там горизонтальный),
   или деликатный градиентный underline под числом — на вкус исполнителя,
   сдержанно.
6. **Навыки: 3 варианта на выбор пользователя** (переключатель в секции —
   небольшие кнопки A/B/C, выбор запоминается в localStorage; текущая сфера
   удаляется):
   A «Категории»: группы чипов по темам с заголовками-киккерами
     (Core: Unity, C#, ECS/DOTS, Jobs/Burst; Multiplayer & Architecture:
     Client-Server, Event Sourcing, Clean Architecture, SOLID, DI;
     Performance: профилирование, мобильная оптимизация, Addressables,
     AssetBundles, URP; Leadership & Process: найм и собеседования,
     менторство, код-ревью, спринт-планирование, Agile, Git, CI/CD;
     AI: AI-assisted development, Claude Code, Codex, AI-пайплайн);
     чипы с hover-подсветкой, категории с reveal-stagger.
   B «Созвездия»: SVG-карта: навыки — звезды (размер по весу), сгруппированы
     в 4-5 «созвездий»-категорий, соединены линиями; hover звезды подсвечивает
     ее созвездие; подписи всегда видны (это контент, не декор); мобайл —
     вертикальная компоновка созвездий.
   C «Бегущие ряды»: 2-3 горизонтальные marquee-ленты чипов (встречные
     направления, пауза на hover), категории перемешаны, скорость ~30px/s.
   Все варианты используют РАСШИРЕННЫЙ список навыков (добавить из опыта:
   Event Sourcing, Addressables, AssetBundles, Profiling, Data migrations,
   Live-ops, Meta/F2P systems, Prototyping, Hiring & Interviews, Sprint
   Planning, Analytics). EN/RU локализация подписей категорий.
   Переключатель дискретный (mono, muted), деплоим все три.

## Итерация 8 (фидбек 2026-07-09): фиксы, контент-харвест, Steam-стиль страниц

### Карусель
1. БАГ: клик по карточке не открывает модалку (сломано drag-скроллом) —
   диагностировать в реальном флоу и починить (порог клик/свайп — стандартный
   паттерн: подавлять клик только при фактическом движении > порога; проверить,
   что suppression-флаг сбрасывается и клик без движения проходит).
2. Инерция мышиного драга: velocity-tracking при отпускании + плавное затухание
   (momentum ~0.95/кадр, остановка на пороге), затем возврат к marquee.

### Навыки → «Астероидный пояс»
3. Пользователь выбрал вариант C. Убрать варианты A/B и переключатель
   (A остается скрытым семантическим fallback для no-JS/SEO — visually-hidden).
4. Усилить эффект пояса (по желанию, вкус): вариации вертикального офсета
   и размера чипов (как астероиды разных размеров), возможно 3 ряда с разной
   скоростью/плотностью.
5. Ключевые навыки (Core: Unity, C#, ECS/DOTS, Jobs/Burst) — цветовой акцент
   (оранжевая рамка/текст). Края лент: fade-градиенты шире и мягче.
6. Интеграция: раздел перестает быть отдельной секцией с заголовком —
   становится полноширинной лентой-разделителем МЕЖДУ «Обо мной» и «Проектами»
   (маленький mono-киккер «Skills» / «Навыки» вместо h2; id="skills" сохранить
   для якоря; пункт «Навыки» из nav УБРАТЬ — меню станет короче).

### Обо мне
7. Убрать градиентные полоски под цифрами метрик.
8. Переписать текст: по абзацу на веху/компанию (Black Games → Reaction Games /
   Dead Impact → Taptap Studio → сейчас: Days After + Forsaken Kingdom),
   легкий космический флер в формулировках (без перебора), EN/RU, без «ё».

### Карточки проектов
9. Ярлык роли — в строке напротив названия игры (справа от h3). Консистентные
   роли-бейджи: days-after «Developer»/«Разработчик», dead-impact
   «Developer»/«Разработчик», mafia-stories «Lead»/«Лид», last-wish
   «Tech Lead»/«Техлид», forsaken-kingdom «Solo programmer»/«Единственный
   программист». Без «Unity»/«Senior» в бейджах.
10. Days After вышел на ПК (VK Play, Steam планируется): platforms += PC;
    links += { label: "VK Play", url: "https://vkplay.ru/play/game/daysafter-20089/" };
    вытащить полезный контент со страницы VK Play (скрины/описание).
11. Last Wish: официальный трейлер youtube FGDIzf8SE6w — скачать его обложку
    (maxresdefault) и поставить ПЕРВЫМ изображением/обложкой карточки.

### Контент-харвест (пользователь явно санкционировал yt-dlp и скачивание
   промо-материалов собственных проектов со сторов/сайтов)
12. Установить yt-dlp (winget/pip). Скачать: трейлер Last Wish (FGDIzf8SE6w),
    официальные видео Days After с канала @days_after (трейлер 73g8uRqtGjA +
    1-2 геймплейных), сжать в webm ≤10МБ каждый.
13. Обойти сторы/сайты всех 5 проектов (deadimpact.com, days-after.com,
    VK Play страницы Days After и Last Wish, GP/AppStore, Steam FK) — выкачать
    доп. скрины/арты, которых еще нет (дедуп с существующими), в галереи.
14. Папка «НА РАЗБОР» в корне: DI_Poster.png → обложка карточки dead-impact
    (конвертировать webp, также рассмотреть как hero-изображение страницы);
    Mafia_banner.png → обложка mafia-stories. Папку удалить после разбора.
15. Прототипы: доп. видео в модалке — для прототипов, у которых в архиве
    несколько роликов, добавить clip-2.webm/clip-3.webm (до 3 доп.),
    в модалке — вертикальный список видео (или мини-плейлист).

### Страницы проектов — Steam-стиль медиа-вьювер
16. Редизайн медиа-блока по образцу Steam (скрин приложен пользователем):
    большой вьювер (видео или скрин) + под ним лента миниатюр (первая —
    видео с ▶, дальше скрины) со стрелками; клик по миниатюре меняет контент
    вьювера; активная миниатюра подсвечена. Видео: локальные webm (трейлеры
    из п.12) и/или youtube-embed. Галерея-лайтбокс может остаться для зума.
    Обе локали, мобайл: вьювер на всю ширину, миниатюры скроллируемые.

### Разное
17. SocialIcons += VK (https://vk.com/sankaaa63), инлайн-SVG иконка VK.
18. Диагностика: приглушение звезд к центру возможно не работает (перекрывается
    twinkle-анимацией?) — проверить фактические альфы в реальном рендере,
    починить если множитель теряется.

## Эксперимент «Орбиты» (ПОСЛЕ итерации 8, отдельно)

Альтернативная версия сайта на отдельном URL (/orbit/), БЕЗ ссылок с основной
и на основную. Полная свобода импровизации. Концепция: профессиональный путь
как планетная система в хронологии — сверху (сейчас) вниз (прошлое):
- Каждая веха карьеры = орбита с планетой-проектом (Days After, Forsaken
  Kingdom, Mafia Stories, Dead Impact, Last Wish, Black Games-эпоха);
- Скиллы = астероидный пояс между орбитами;
- Прототипы = облако мелких астероидов/спутников в конце (эпоха Black Games);
- Скролл = путешествие от внешних орбит к центру звезды (или наоборот);
- Планеты кликабельны (детали проекта), стилизация: орбитальные кольца,
  разные размеры/цвета планет по масштабу проекта.
RU-локали достаточно для эксперимента. Технически: отдельная страница Astro
(src/pages/orbit.astro) со своим стилем/скриптами, не затрагивая основной сайт.

## Итерация 9 (фидбек пользователя от 2026-07-09, основная версия)

### Тексты и структура
1. Убрать космическую лексику из ТЕКСТОВ (космос — только в дизайне):
   About-вехи «first orbit of my career» / «putting it into orbit» /
   «стартовая орбита карьеры» / «выведя его на орбиту» — заменить обычными
   формулировками без метафор. Проверить весь src на подобные обороты
   (страница /orbit/ не в счет — там тема сознательная).
2. Навыки: убрать видимый заголовок-кикер «Навыки»/«Skills» над лентой,
   но ВЕРНУТЬ пункт «Навыки» в навигацию шапки (якорь #skills остается,
   scroll-spy должен его подсвечивать).
3. Прототипы: больше НЕ отдельный раздел. Пристыковать карусель в конец
   раздела «Проекты» (внутрь section#projects, после сетки карточек), без
   h2 «Архив прототипов» и без спец-заголовков. Подводку-строку можно
   оставить/сократить как тихий текст, решить по вкусу. Пункт «Прототипы»
   из навигации убрать. Смысл: смотрится как более мелкие проекты в
   альтернативном визуале (там есть и милионник Car Junk Resurrection).
4. Футер: убрать «All rights reserved.» — юридически мертвая фраза, и у
   автора нет прав на сами игры. Оставить «© 2026 Aleksandr Kandakov»
   (копирайт самого сайта; клик-пасхалка с мини-игрой остается).
5. Роли-бейджи: НЕ переводить, английские в обеих локалях. Новый набор:
   days-after/dead-impact «Developer», mafia-stories «Team Lead»,
   last-wish «Tech Lead», forsaken-kingdom «Tech Lead» (вместо
   «Единственный программист» — консистентнее и отражает суть).

### Багфиксы
6. Лента навыков (marquee): баг зацикливания — контент занимает не всю
   ширину ленты, пустота доезжает до середины и контент «резко появляется
   на всей длине». Причина: группа чипов уже вьюпорта (двух групп при
   translateX(-50%) не хватает на широких экранах). Фикс: дублировать
   контент группы столько раз, чтобы одна группа гарантированно была шире
   максимального вьюпорта (или JS-замер), шов должен быть бесшовным.
7. Hover-слайдшоу карточек проектов: при первом наведении скрины листаются
   хаотично/быстро (по мере догрузки), потом штатно. Фикс: предзагрузка,
   смена кадра только после фактической загрузки следующего изображения
   (img.decode()/load), равные интервалы с самого начала.

### Полировка
8. Кнопки контактов (CV + социконки в hero и в Contact): периодический
   пробегающий блик (sheen) — по кнопкам по очереди, ненавязчиво.
9. Монограмма «AK» в шапке: чуть крупнее; подсветка не квадратом-подложкой,
   а внутренним свечением самих букв (glow изнутри).

### Steam-вьювер (MediaViewer)
10. Изображения в сцене должны заполнять все пространство (сейчас
    динамически созданные img/video теряют Astro-scoped стили и жмутся в
    угол — починить стилизацию динамических элементов).
11. Слайдшоу: скрины сменяются автоматически раз в ~3с; видео — после
    окончания проигрывания (для youtube — через enablejsapi/postMessage
    либо не автопродвигать, если ненадежно). Пауза/перезапуск таймера при
    взаимодействии пользователя (лайтбокс, ручной выбор).
12. Тот же вьювер — в модалке прототипов (вместо вертикального списка
    видео + сетки скринов): большой стейдж + лента миниатюр; переиспользовать
    логику общим клиентским скриптом (SSR-вьювер страниц проектов и
    JS-собранный вьювер модалки).

### Контент
13. «НА РАЗБОР»: DA_Poster.avif (626×352) → cover.webp карточки days-after;
    FK_Poster.jpg (616×353) → cover.webp forsaken-kingdom. Формат мелковат —
    аккуратно конвертировать (webp q85+, при необходимости мягкий апскейл
    x1.5 lanczos + unsharp, смотреть глазами). Папку удалить после разбора.
14. Days After: убрать shot-1 (первый после видео) и shot-7 (последний),
    оставшиеся перенумеровать в shot-1..5.
15. Last Wish: обложка карточки — с youtube c3MGKW_LXAU
    (i.ytimg.com/vi/c3MGKW_LXAU/maxresdefault.jpg, 1280×720, доступна) →
    cover.webp (заменить текущую).

## Эксперимент «Орбиты» v2 — интерактивная карта звездной системы (фидбек 2026-07-09)

Пользователю первая версия понравилась («Супер!»), теперь редизайн /orbit/:
1. Превратить страницу в ПОДОБИЕ ИНТЕРАКТИВНОЙ КАРТЫ звездной системы
   (не вертикальный скролл-рассказ, а карта/сцена).
2. Центр системы — ЗВЕЗДА-ВИЗИТКА (Александр Кандаков, Senior Unity
   Developer, портрет) — а не первое место работы.
3. Орбиты раскручиваются ОТ ПОСЛЕДНЕГО К ПЕРВОМУ: ближняя к звезде —
   «Сейчас» (2026 — н.в.), дальше Taptap (2026), Reaction Games (2023-2026),
   внешняя — Black Games (2020-2023).
4. Превью проектов (планеты) — КРУПНЕЕ, чем в v1.
5. Остальное — полный фристайл: «Импровизируй, попробуй сделать красиво,
   современно и презентабельно». Прежний вкусовой ориентир действует:
   выразительно, без цыганщины.
6. КОНТЕНТ согласовать с основной версией (тексты вех About, роли-бейджи
   английские Developer/Team Lead/Tech Lead, метрики, ссылки, обложки
   cover.webp, копирайт без «All rights reserved»), форма подачи — свободная.
7. Ограничения прежние: /orbit/ изолирован (ноль ссылок с/на основной сайт),
   RU, noindex, свои файлы (orbit.astro/orbit.css/orbit.js/orbit-stars.js),
   основной сайт не трогать; переиспользовать общие модули (media-viewer.js)
   можно. Правила: без «ё», без reduced-motion гейтов.

## Итерация 10 (фидбек 2026-07-09: полировка карты /orbit/ + интеграция + FK-видео)

### Карта /orbit/ — полировка (v2.1)
1. Планеты накладываются друг на друга, особенно при отдалении. Идея
   пользователя: контр-масштаб с ВЕСОМ («как параллакс, только с размером») —
   вместо константного экранного размера scale(1/zoom) использовать частичный
   контр-масштаб scale(zoom^-w), w≈0.6-0.7: при отдалении маркеры умеренно
   уменьшаются — меньше пересечений. Дополнительно развести стартовые углы
   планет (на одной орбите — противофаза, соседние орбиты — разные фазы),
   чтобы наложения были редкими.
2. Чипы пояса навыков перекрывают планеты — слои: пояс ПОД планетами
   (z-index), допустимо слегка приглушить чипы.
3. Скорости: прототипы двигаются слишком медленно — ускорить заметно;
   большие проекты замедлить ~в 2 раза; всем дать слегка РАЗНЫЕ скорости
   (интереснее, живее).
4. Звезда-визитка: изображение солнца (диск/свечение) ЗА фоткой — лишнее,
   убрать; сам элемент звезды сделать крупнее.
5. Постоянные подписи: короче (только название проекта; у звезды — только
   имя) и скрывать (fade) при слишком далеком масштабе, чтобы не шумело.
6. Короткие пояснительные блоки при наведении на планеты и на фотку:
   анимированное появление, пара строк (например роль · годы · метрика).
6b. Астероиды-прототипы с иконками на прозрачном фоне смотрятся странно —
   нужна подложка (например круглая поверхность/диск под иконкой в стиле
   сцены), чтобы каждый астероид читался цельным объектом.
6c. Фокус-режим орбит: при выборе орбиты в навигации (легенде) остальные
   орбиты приглушаются (меньше/темнее, без подписей), выбранная — в фокусе.
   В легенду добавить дефолтный пункт «вся система» (обзор как сейчас) —
   активен по умолчанию и возвращает сцену из фокус-режима.
6d. Анимация «варп-прыжок» при открытии окон проектов и прототипов
   (открытие модалки = прыжок: рывок камеры/звезд + появление окна).
6e. Окно проекта на карте: тоже Steam-like вьювер (общий media-viewer:
   видео-трейлеры + скрины проекта) и полный набор контента (обложка,
   роль, годы, жанр, метрики, описание, ссылки сторов) + ссылка на
   страницу проекта основной версии (/ru/projects/<slug>/).

### Интеграция карты с основным сайтом
7. Название: «Интерактивная карта опыта» (EN: «Interactive experience map»).
   Убрать со страницы /orbit/ формулировки «эксперимент/альтернативная
   версия» (kicker, подзаголовок, футер, <title>). Снять noindex — теперь
   это официальный раздел. Обратная ссылка с карты на основной сайт
   (ненавязчиво, HUD).
8. На основном сайте — БЕЗ пункта в навигации (пользователь сомневался,
   решение: nav и так плотный на мобиле; виджет достаточно заметен).
   Интеграция «в стиле гугл-карт» (уточнение пользователя): виджет-миникарта
   — превью звездной системы (орбитальные кольца, планеты-точки, легкая
   анимация) как embed-карта на сайтах, с кнопкой/оверлеем «Открыть карту» —
   между «Проектами» и «Контактами», обе локали; на EN пометка, что карта
   на русском. Название: «Интерактивная карта опыта».
8b. На страницах проектов основной версии — кнопка «Показать на карте» в
   стиле навигационной иконки (пин/миникарта, как «open in maps»), ведущая
   на /orbit/ с deep-link фокусом на планету этого проекта
   (например /orbit/#focus=<slug>; orbit.js после загрузки фокусирует
   камеру на планете). Обратная связка — п.6e (из окна проекта на карте
   ссылка на страницу проекта).

### Контент FK
9. «НА РАЗБОР»: 4 превью-ролика Forsaken Kingdom (960x540 vp9 webm, 6.7-12.7с,
   0.5-0.9МБ) → public/media/projects/forsaken-kingdom/trailer.webm,
   trailer-2..4.webm (порядок: gameplay, battle, placement, upgrade).
   MediaViewer: жесткий список ['trailer.webm','trailer-2.webm'] заменить
   fs-сканом /^trailer(-\d+)?\.webm$/ с числовой сортировкой. Папку удалить.

## Итерация 11 (фидбек 2026-07-15)

### Основная страница
1. Hover-слайдшоу карточек: при смене скринов на долю секунды проступает
   обложка (кроссфейд двух слоев: оба полупрозрачны в середине перехода —
   просвечивает cover под ними). Фикс: фейдить один скрин прямо в другой —
   новый слой поверх старого (старый остается непрозрачным до конца
   перехода, затем скрывается без transition; чередование z-index);
   вариант «через черноту» — только если напрямую не выйдет.
2. Steam-вьювер: смена слайда (в т.ч. автослайдшоу) перематывает страницу
   к ленте миниатюр, если пользователь вверху страницы. Причина:
   thumbs[active].scrollIntoView(block:'nearest') скроллит СТРАНИЦУ.
   Фикс: ручной горизонтальный скролл ленты (strip.scrollTo по вычисленному
   scrollLeft), страницу не трогать. Общий media-viewer.js — фикс улучшит
   и карту.
3. Ссылки на интерактивную карту — стандартной иконкой геолокации
   («капелька с дыркой», map-pin): кнопка «Показать на карте» на страницах
   проектов; НА КАРТОЧКАХ проектов главной — бейдж-пин (как бейджи
   платформ; пользователь предложил правый верхний угол — там уже бейджи
   платформ, разместить рядом с ними или «как типичнее», на вкус
   исполнителя), ведет на /orbit/#focus=<slug>. ВНИМАНИЕ: корень карточки —
   <a>, вложенный <a> невалиден — реструктурировать карточку (div-корень +
   stretched-link или эквивалент), не сломав hover-слайдшоу/lift/reveal.

### Интерактивная карта
4. Кнопку возврата на основной сайт вытащить из легенды (отдельный HUD-
   элемент) и сделать чуть ярче/заметнее.
5. Легенда — сворачивающийся блок (collapse/expand).
6. Пункт «визитка» из легенды убрать.
7. Прототипы-астероиды:
   a. Сейчас колышутся на месте — должны ВРАЩАТЬСЯ по орбите (как планеты),
      но помедленнее ~в 2 раза; скорости у всех разные.
   b. Стартовые позиции с соблюдением минимальной дистанции (сейчас
      перекрывают друг друга; миллионник почти полностью закрыт).
   c. Миллионник (Car Junk Resurrection) — крупнее и поверх остальных
      (отрисовывать последним/z-index).
8. Теги-«спутники» планет: маленькие бейджи, вращающиеся вокруг планеты —
   платформа (Mobile/PC иконкой), масштаб (10M+/1M+/500K+), мультиплеер
   (для соответствующих проектов). Данные из frontmatter. Со вкусом,
   не перегружать.
9. Варп не виден — карточка открывается ДО него. Подстроить тайминги:
   сначала варп-рывок, окно появляется в конце/внахлест.
10. Варп-анимация и на ЗАКРЫТИЕ карточек (обратный переход), тайминги
    согласовать.
11. При закрытии карточки проекта/прототипа легенда возвращается на
    «Вся система».
12. Hover-окно у планеты: (а) должно следовать за движущейся планетой
    (привязка позиции); (б) редизайн в научпоп/sci-fi сноску: точка на
    объекте → линия «ступенькой» (leader line) → текст сноски, с анимацией
    появления (line-draw). Прежний дизайн пользователю не зашел.

## Итерация 12 (фидбек 2026-07-15, второй раунд)

### Основная страница
1. Автовоспроизведение видео в Steam-вьювере: активный видео-слайд играет
   сам (muted; локальные видео — play() muted при attach/смене, youtube —
   autoplay=1&mute=1). Ручной клик по миниатюре — как раньше (со звуком,
   fallback muted).
2. Пин-бейдж геолокации на карточках — другим цветом, отличным от бейджей
   платформ (оранжевые) — например тил (--teal, язык интерактива/навигации).
3. Карусель прототипов: миллионник (pin) не первым, а ~4-м — первый элемент
   сразу уезжает за левый край при появлении секции.
4. Превью прототипов без видео: при наведении показывать слайдшоу скринов
   (фактически один такой — Skee Ball League: clip=false, shots>0).

### Карта
5. Hover-сноска: (а) вертикальный сегмент линии иногда не с той стороны
   (скрин: линия вверх-влево при тексте снизу-справа) — починить геометрию
   флипа для всех квадрантов; (б) подложка под текст с уводом в альфу к
   краю (радиальный/линейный градиент) — сейчас местами плохо читается.
6. Спутники-бейджи планет: крупнее, КРУГЛЫЕ «планетки» (не овальные пилюли),
   ярче — не сливаться с фоном.
7. Миллионнику-прототипу — тоже спутник (1M+), а его подпись убрать
   (срезается).
8. Пульсирующее свечение вокруг фотки-звезды шире самой фотки (скрин) —
   ореол должен облегать фото.
9. Легенда v2: сворачивается ВЕСЬ блок (с рамкой) ВЛЕВО в иконку, как
   панели инструментов. Убрать заголовок «Орбиты» из HUD и подпись
   «Легенда» (для a11y оставить sr-only h1 «Интерактивная карта опыта»).
10. Варп при открытии: звезды устремляются К ТОЧКЕ КЛИКА (или к планете/
    курсору — как проще), эффект прыжка к выбранной планете.

## Вне рамок (пока)

- Кастомный домен (добавим позже: покупка kandakov.dev + CNAME).
- Блог, аналитика, формы обратной связи.
